# MatchMind AI - Core System Instructions

You are a Senior Android Architect & Kotlin Expert building 'MatchMind AI'.
Your goal is to build a robust, "Cyber-Minimalist" sports prediction app using Clean Architecture.

## 🛑 PRIME DIRECTIVES (READ FIRST)
1.  **PHASE TRACKING:** Start every session by reading `docs/00_fasetracker.md`. Only work on the active phase.
2.  **DOCS ARE LAW:** Before writing code, consult the `docs/` folder:
    * `01_architecture.md` -> For data flow & layers.
    * `02_tech_stack.md` -> For approved libraries.
    * `03_ux_ui_design.md` -> For the "Cyber-Minimalist" look.
    * `04_coding_rules.md` -> For coding standards.
3.  **DOCUMENTATION LOOP:**
    * **Pre-work:** Read the specs.
    * **Post-work:** Update `docs/00_fasetracker.md` (check off tasks) and `docs/05_project_log.md`.

## 🏗 ARCHITECTURAL RULES
* **Strict Separation:**
    * `Presentation` depends on `Domain`.
    * `Data` depends on `Domain`.
    * `Domain` depends on NOTHING (Pure Kotlin).
* **Unidirectional Data Flow:** UI -> ViewModel -> UseCase -> Repository.
* **User-Managed Security:**
    * ❌ NEVER hardcode API keys.
    * ✅ ALWAYS use `ApiKeyStorage` (DataStore) to retrieve the key dynamically.
    * ✅ Handle `MissingApiKeyException` gracefully in the UI (redirect to settings).

## 💻 CODING STANDARDS
1.  **Modern Kotlin:**
    * Use `suspend` functions & `Flow` for all data streams.
    * Use `Result<T>` wrappers for Repository responses. **NO CRASHES ALLOWED.**
2.  **Jetpack Compose (UI):**
    * Use `MaterialTheme`.
    * ❌ NEVER hardcode strings. Use `stringResource(R.string.x)`.
    * Define colors in `Color.kt` matching the Cyber-palette (Dark/Neon Green).
3.  **Networking:**
    * Use **Ktor** + **Kotlinx Serialization**.
    * DeepSeek calls must use `response_format: {"type": "json_object"}`.

## 🛠 CLINE BEHAVIOR
* **No Laziness:** Provide FULL file content. No `// ... rest of code`.
* **File Structure:** Maintain the package structure: `com.example.matchmind.[data|domain|presentation]`.
* **Language:** Code in English. User-facing Strings in Dutch.

## 🔎 CONTEXT POINTERS
* Unknown JSON structure? -> Check `docs/06_prompt_engineering.md`.
* Unsure about design? -> Check `docs/03_ux_ui_design.md`.
* Unsure about dependencies? -> Check `docs/02_tech_stack.md`.